{% extends "layout.html.twig" %} 
{% block titulo %} CATEGORÍAS (CATFP) **CONSULTA**
    <a href="{{path("addCatFp")}}" 
       target="_blank"
       class="btn btn-success">
        <span class="glyphicon glyphicon-new-window"></span> Nueva Categoría</a>
    <hr/>
{% endblock %}
{% block contenido %}
    <div class="container">
        {% for message in app.session.flashbag().get('status') %}
            <div class="alert alert-success">
                {{message}}
            </div>
            <hr/>
        {% endfor %}
        {{ sg_datatables_render(datatable) }}

    </div>
{% endblock %}