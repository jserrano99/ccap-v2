{% extends "layout.html.twig" %} 
{% block titulo %} ASIGNACIÓN DE CENTROS DE COSTE A PLAZAS  **CARGA FICHEROS**
    <a href="{{path("queryCeco")}}" class="btn btn-success">Volver</a>
{% endblock %}

{% block contenido %}
    <script>
        $(document).ready(function () {
            $("#importar_Guardar").click(function () {
                let recurso;
                $("#procesando").modal('show');
                recurso = Routing.generate("importaCecoCias", true);
                $.ajax({
                    type: "POST",
                    url: recurso,
                    success: function (data, status, xhr) {
                        $('.modal-backdrop').remove();
                        $('#procesando').hide();
                    },
                    error: function (xhr, ajaxOptions, thrownError) {
                        $('#procesando').hide();
                        alert(xhr.status);
                    }
                });
            });
        });
    </script>
    <div class="container">
        {% for message in app.session.flashbag().get('status') %}
            <div class="alert alert-success">
                {{message}}
            </div>
        {% endfor %}
        <h3>FORMATO DEL FICHERO</h3>
        <br/>
        <img src= "{{asset('img/FormatoCargaCecoCias.jpg')}}"></img>  

        {{form_start (form,{'action':'','method':'POST'})}}
        {{form_end (form)}}
        
        <br/>
    </div>
{% endblock %}