{% extends "layout.html.twig" %} 
{% block titulo %} PLAZAS **CAMBIO DE ADSCRIPCION  MASIVA**
{% endblock %}

{% block contenido %}
    <script>
        $(document).ready(function () {
            $("#importar_Guardar").click(function () {
                let recurso;
                $("#procesando").modal('show');
                recurso = Routing.generate("cambioAdscripcion", true);
                window.location = recurso;
            });
        });
    </script> 

    <div class="container">
        {% for message in app.session.flashbag().get('status') %}
            <div class="alert alert-success">
                {{message}}
            </div>
        {% endfor %}
        <h3>FORMATO DEL FICHERO </h3>
        <br/>
        <img src= "{{asset('img/cambio.adscrip.format.JPG')}}"></img>

        {{form_start (form,{'action':'','method':'POST'})}}
        {{form_end (form)}}
        <br/>

    </div>
{% endblock %}