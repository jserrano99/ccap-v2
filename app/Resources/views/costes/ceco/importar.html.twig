{% extends "layout.html.twig" %} 
{% block titulo %} CONTROL CENTROS DE COSTE **CARGA FICHEROS**
{% endblock %}

{% block contenido %}
    <script>
        $(document).ready(function () {
            $("#importar_Guardar").click(function () {
                $("#procesando").modal('show');
                var recurso = Routing.generate("cargaFichero", true);
                window.location = recurso;
            });
        });
    </script> 

    <div class="container">
        {% for message in app.session.flashbag().get('status') %}
            <div class="alert alert-success">
                {{message}}
            </div>
        {% endfor %}
        <h3>FORMATO DEL FICHERO </h3>
        <br/>
        <img src= "{{asset('img/FormatoFichero.JPG')}}"></img>  

        {{form_start (form,{'action':'','method':'POST'})}}
        {{form_end (form)}}
        <br/>

    </div>
{% endblock %}