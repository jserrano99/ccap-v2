{% extends "layout.html.twig" %}
{% block titulo %} ASIGNACIÓN DE PLAZA A UNIDAD ORGANIZATIVA:{{unidadOrganizativa.descripcion|upper}}
    {#<a href="{{ path("addUnidad") }}"#}
       {#class="btn btn-success">#}
        {#<span class="glyphicon glyphicon-th-list"></span> Volver </a>#}
{% endblock %}
{% block contenido %}
    <script>
        $(document).ready(function () {
        });
    </script>

    <div class="container">
        {% for message in app.session.flashbag().get('status') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}

        {{ form_start(form) }}
        {{ form_end(form) }}
    </div>

{% endblock %}