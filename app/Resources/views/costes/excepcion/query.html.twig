{% extends "layout.html.twig" %} 
{% block titulo %}** CONSULTA EXCEPCIONES EN ASIGNACION DE CECOS **
    <a href="{{path("addExcepcion")}}" class="btn btn-success">
        <span class="glyphicon glyphicon-new-window"></span> Nueva Excepción</a>
    <a href="{{path("importaExcepcion")}}" class="btn btn-success"
       data-toggle="tooltip" 
       data-placement="bottom" 
       title="Cargar Fichero">
        <span class="glyphicon glyphicon-floppy-save"></span> Cargar Fichero</a>
    <hr/>
{% endblock %}
{% block contenido %}
    <div class="container">
        {% for message in app.session.flashbag().get('status') %}
            <div class="alert alert-success">
                {{message}}
            </div>
            <hr/>
        {% endfor %}
        {{ sg_datatables_render(datatable) }}
    </div>
{% endblock %}