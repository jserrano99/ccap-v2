{% extends "layout.html.twig" %} 
{% block titulo %} PROCESO TERMINADO 
{% endblock %}

{% block contenido %}
    <div class="container">
        {% for linea in resultado %}
                Ceco Calculado:{{linea.cecoReal}}- {{linea.cecoExcepcion}}
                Resultado:{{linea.estado}}-{{linea.error}}
            <br/>
        {% endfor %}
        <hr/>
        <a href="{{path("queryExcepcion")}}" class="btn btn-success">Volver</a>
    </div>
{% endblock %}