{% extends "layout.html.twig" %} 
{% block titulo %} CARGA DE EXCEPCIONES EN LA ASIGNACIÓN DE CENTROS DE COSTE 
    <a href="{{path("queryExcepcion")}}" class="btn btn-success">Volver</a>
{% endblock %}

{% block contenido %}
    <div class="container">
        {% for message in app.session.flashbag().get('status') %}
            <div class="alert alert-success">
                {{message}}
            </div>
        {% endfor %}
        <h3>FORMATO DEL FICHERO </h3>
        <br/>
        <img src= "{{asset('img/FormatoCargaExcepcion.jpg')}}"></img>  

        {{form_start (form,{'action':'','method':'POST'})}}
        {{form_end (form)}}
        <br/>
    </div>
{% endblock %}