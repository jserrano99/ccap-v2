{% extends "layout.html.twig" %} 
{% block titulo %} BASES DE DATOS **CONSULTA**
    <a href="{{path("addBaseDatos")}}" 
       class="btn btn-success"
       target="_blank">
        <span class="glyphicon glyphicon-new-window"></span> Nueva Base de Datos</a>
    <hr/>
{% endblock %} 

{% block contenido %}
    <div class="container">
        {% for message in app.session.flashbag().get('status') %}
            <div class="alert alert-success">
                {{message}}
            </div>
            <hr/>
        {% endfor %}
        {{ sg_datatables_render(datatable) }}
    </div>
{% endblock %}